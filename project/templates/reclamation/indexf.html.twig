{% extends 'base-front.html.twig' %}



{% block content %}
<br>
<br>
 <center>  <br>
     <br>
     <a href="{{ path('frontcreatereclamation') }}"> <button class="btn-primary"> Create new</button></a>
     <br>
     <br>
 </center>
    <div class="mahdi">
    <table class="table">
        <thead>
            <tr>
                <th>Message</th>
                <th>Status</th>
                <th>Type</th>
                <th>user</th>
                <th>reservation</th>
                <th>actions</th>

            </tr>
        </thead>
        <tbody>
        {% for reclamation in reclamations %}
            <tr>
              <!--  <td>{{ reclamation.id }}</td> -->
                <td>{{ reclamation.message }}</td>
                <td>{{ reclamation.status }}</td>
                <td>{{ reclamation.type }}</td>
               <td>{{ reclamation.user.name }}</td>
                <td>{{ reclamation.reservation.id }}</td>
                <td>
                    <a href="{{ path('reclamation_show', {'id': reclamation.id}) }}"><button class="btn-primary"> show </button></a>
                  <!--  <a href="{{ path('reclamation_edit', {'id': reclamation.id}) }}">edit</a> -->
                
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}
