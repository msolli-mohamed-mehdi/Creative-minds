{% extends 'base-front.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block content %}
<br>
<br>
<br>
<br>
    <center>     <h1>Reclamation on reservation  {{ reclamation.reservation.id }}</h1>
        <br>
<div class="mahdi">
    <table class="table">
        <tbody>
        <!--  <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr> -->
            <tr>
                <th>Message</th>
                <td>{{ reclamation.message }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reclamation.status }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type }}</td>
            </tr>
            <tr>
            <th>reservation name</th>
            <td>{{ reclamation.reservation.id }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a href="{{ path('frontviewreclamation') }}"> <button class="btn-primary"> back to list</button></a>

  <!--  <a href="{{ path('reclamation_edit', {'id': reclamation.id}) }}">edit</a>-->
<br>
        <br>
    {{ include('reclamation/_delete_form.html.twig') }}
    </center>
{% endblock %}
